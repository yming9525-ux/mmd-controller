# 更新日志

## v10.4.4

**控制器显示拆细**

原来只有身体、四肢两个显示开关，现在拆成六个：身体、手指手捩、手 FK、手 IK、脚 FK、脚 IK。想单看手上的 IK 就只开手 IK，视口清爽很多。骨骼集合跟着拆成 `MMD_Arm_FK_Controllers`、`MMD_Arm_IK_Controllers`、`MMD_Leg_FK_Controllers`、`MMD_Leg_IK_Controllers` 四个。

旧工程会自动迁移。打开老文件时插件会把已经生成的四肢控制器归到新集合里，不用重新生成。

**IK 目标跟随总控**

手腕 / 脚踝的 IK 目标改挂在「全ての親」下面。拖总控移动整个角色时，IK 目标跟着走，手脚不再被留在原地。IK 目标自己的位移和旋转照常独立。

**方向球跟着末端走**

手肘 / 膝盖的方向球（极向量）改挂在对应的 IK 目标下面，保留局部偏移。拖手脚的时候方向球一起动，不用再手动追着调。

## v10.4.3

### 修复

1. 预弯曲导致蒙皮变形、FK 模式异常。弯曲机制整个重做，所有弯曲只发生在 IK 控制链内部，模型骨骼从头到尾不被修改，蒙皮零影响
2. 脚尖控制器（`IKT_足先EX`）姿态异常。改为纯旋转手柄，位置自动跟随脚踝
3. 标准模型的中心骨识别不到。`センター` 和 `センター2` 都能识别，同时存在时层级自动衔接
4. 被旧版物理预弯过的模型上「校准弯向」失效。校准会先吃掉模型自带的弯再朝校准方向偏移，弯量自动匹配；切换 IK/FK 时也不再把模型固有的弯误认成用户姿势
5. VPD 导入后姿势不完整（腿直挺、耸肩丢失）。腿部姿势藏在 `足ＩＫ` 骨里，吸附时临时恢复原生足 IK 参与解算，`肩P` 等外围骨的贡献一并吸收
6. VPD 导出后在 MMD 里腿被拉直、导出再导入姿势对不上。导出时把脚踝 / 脚尖位置反推写进 `足ＩＫ` 和 `つま先ＩＫ`，导出导入闭环误差为零
7. VPD 往返后手指弯曲叠加。吸附时 BEND 球自动归零
8. IK 模式下蹲时脚部蒙皮轻微滑移（D 骨模型）。`足首D` 和 `ひざD` 生成时自动加位置钉子跟住本尊，移除系统时自动拆除

### 新增

1. 弯向自动判定。生成时按模型手指、脚尖方向自动判断手肘膝盖弯向，模型朝向任意；没有手指骨的模型退回默认方向
2. 自动弯一点 FK 加按当前姿势校准弯向。四肢按判定弯向各折 15°，弯向对错一眼看穿，支持单肢和一键全部，全程不改模型骨骼
3. VPD / VMD 一条龙导入导出（需装 mmd_tools）。导入 VPD 姿势含腿部 IK 转 FK；导出 VPD 自动回写，闭环无损；导入 VMD 逐帧吸附到控制器，表情直接生效，帧率帧范围自动对齐 30fps；导出 VMD 自动烘焙含表情，可选导出后清理烘焙关键帧
4. IK/FK 一键切换。每条肢体一行，滑条加 `⇄` 切换加 `◆` K 帧，顶部「一键全部切换」
5. 姿势全部归零。控制器、原骨、BEND 一键回默认姿势
6. `肩P` 控制器。耸肩骨纳入控制体系，VPD / VMD 里的肩P 姿势可见可清
7. 自动接管 MMD 原生足部 IK。生成时自动关闭 `足ＩＫ` 和 `つま先ＩＫ`，移除系统时恢复
8. 控制器形状自动打包，统一收进隐藏集合「MMD_控制器形状」，移除时一并清理
9. 插件偏好设置「显示哪些模块」，五个区块可单独显隐

### 变更

1. 「预弯曲手肘膝盖 / 恢复直立」预处理移除，不再需要生成前的准备步骤
2. 「四指张开 / 并拢」面板移除，功能并入 BEND 球，选中按 <kbd>S</kbd> 缩放即张开并拢
3. 「导出姿态 / 导入姿态」（JSON）按钮撤出面板，功能保留，<kbd>F3</kbd> 可搜。日常姿势进出走 VPD

老工程升级后重新点一次「生成骨骼系统」，刷新手指驱动并补出 `肩P` 控制器。

## v10.4.1

基线版本。
